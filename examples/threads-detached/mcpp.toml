[package]
name    = "threads-detached"
version = "0.1.0"

[dependencies]
openkal-musl = { path = "../.." }

[targets.threads-detached]
kind = "bin"
main = "src/main.c"

[build]
cxx_runtime = "host-coupled"
